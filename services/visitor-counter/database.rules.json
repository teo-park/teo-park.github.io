{
  "rules": {
    ".read": false,
    ".write": false,
    "counters": {
      ".read": "auth != null && auth.token.email === 'teo.ffxiv.kr@gmail.com' && auth.token.email_verified === true && auth.token.firebase.sign_in_provider === 'google.com'"
    },
    "visits": {
      ".read": "auth != null && auth.token.email === 'teo.ffxiv.kr@gmail.com' && auth.token.email_verified === true && auth.token.firebase.sign_in_provider === 'google.com'",
      "home": {
        "$eventId": {
          ".write": "!data.exists() && newData.val() === true && $eventId.matches(/^[a-f0-9]{32}$/)",
          ".validate": "newData.getPriority() === null"
        }
      },
      "msq-tracker": {
        "$eventId": {
          ".write": "!data.exists() && newData.val() === true && $eventId.matches(/^[a-f0-9]{32}$/)",
          ".validate": "newData.getPriority() === null"
        }
      },
      "pvp-series-calculator": {
        "$eventId": {
          ".write": "!data.exists() && newData.val() === true && $eventId.matches(/^[a-f0-9]{32}$/)",
          ".validate": "newData.getPriority() === null"
        }
      },
      "duty-finder": {
        "$eventId": {
          ".write": "!data.exists() && newData.val() === true && $eventId.matches(/^[a-f0-9]{32}$/)",
          ".validate": "newData.getPriority() === null"
        }
      },
      "triple-triad": {
        "$eventId": {
          ".write": "!data.exists() && newData.val() === true && $eventId.matches(/^[a-f0-9]{32}$/)",
          ".validate": "newData.getPriority() === null"
        }
      },
      "minions": {
        "$eventId": {
          ".write": "!data.exists() && newData.val() === true && $eventId.matches(/^[a-f0-9]{32}$/)",
          ".validate": "newData.getPriority() === null"
        }
      },
      "blue-mage": {
        "$eventId": {
          ".write": "!data.exists() && newData.val() === true && $eventId.matches(/^[a-f0-9]{32}$/)",
          ".validate": "newData.getPriority() === null"
        }
      },
      "beastmaster": {
        "$eventId": {
          ".write": "!data.exists() && newData.val() === true && $eventId.matches(/^[a-f0-9]{32}$/)",
          ".validate": "newData.getPriority() === null"
        }
      },
      "beastmaster__guide": {
        "$eventId": {
          ".write": "!data.exists() && newData.val() === true && $eventId.matches(/^[a-f0-9]{32}$/)",
          ".validate": "newData.getPriority() === null"
        }
      },
      "weapons": {
        "$eventId": {
          ".write": "!data.exists() && newData.val() === true && $eventId.matches(/^[a-f0-9]{32}$/)",
          ".validate": "newData.getPriority() === null"
        }
      },
      "fisher-skills": {
        "$eventId": {
          ".write": "!data.exists() && newData.val() === true && $eventId.matches(/^[a-f0-9]{32}$/)",
          ".validate": "newData.getPriority() === null"
        }
      },
      "fisher-skills__big-fish": {
        "$eventId": {
          ".write": "!data.exists() && newData.val() === true && $eventId.matches(/^[a-f0-9]{32}$/)",
          ".validate": "newData.getPriority() === null"
        }
      },
      "fishing-log": {
        "$eventId": {
          ".write": "!data.exists() && newData.val() === true && $eventId.matches(/^[a-f0-9]{32}$/)",
          ".validate": "newData.getPriority() === null"
        }
      },
      "ocean-fishing": {
        "$eventId": {
          ".write": "!data.exists() && newData.val() === true && $eventId.matches(/^[a-f0-9]{32}$/)",
          ".validate": "newData.getPriority() === null"
        }
      },
      "ocean-fishing__checklist": {
        "$eventId": {
          ".write": "!data.exists() && newData.val() === true && $eventId.matches(/^[a-f0-9]{32}$/)",
          ".validate": "newData.getPriority() === null"
        }
      },
      "ocean-fishing__sources": {
        "$eventId": {
          ".write": "!data.exists() && newData.val() === true && $eventId.matches(/^[a-f0-9]{32}$/)",
          ".validate": "newData.getPriority() === null"
        }
      }
    }
  }
}
